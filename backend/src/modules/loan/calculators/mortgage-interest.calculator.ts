import { ReducingBalanceInterestCalculator } from "./reducing-balance.calculator";

export class MortgageInterestCalculator extends ReducingBalanceInterestCalculator {
  // Currently, uses same logic as reducing balance
  // Customize here for mortgage-specific rules if needed
}
